# 数学建模工作区使用说明

这是一套“自然语言驱动、人工把关、全程可复现”的竞赛工作区。正常使用时，
队员只需要把材料放进正确目录并告诉 Codex 要做什么，**不需要手动点名 Skill 或 Agent**。
Codex 会先读取项目状态，再自动选择研究、批判、实现、优化、验证、制图或论文角色。

## 1. 开始一道新题

1. 官方题目、附件和比赛要求放入 `01_题目与要求/`。
2. 队伍已有论文、教材和参考材料放入 `02_参考文献/`。
3. 对 Codex 说：

   > 接管这道新题。先检查环境、题目附件和数据完整性，更新状态；不要开始解题，列出 G1 需要我确认的选题信息。

Codex 会自动完成状态检查、输入哈希和资料盘点。看到问题选择建议后，由人明确回复
“批准 G1：选择……”。任何 Agent、测试或 Hook 都不能替你通过 Gate。

## 2. 标准竞赛流程

| 阶段 | 直接这样说 | 系统会做什么 | 人工 Gate |
| --- | --- | --- | --- |
| 理解题目 | “拆解问题、变量、单位、目标、约束和隐含要求” | 问题分析、数据需求和假设账本 | G1 选题 |
| 数据与文献 | “审计附件数据，并核验需要的文献和参数来源” | 数据质量、泄漏、证据与引用核验 | — |
| 比较方法 | “比较这几条候选建模路线，做有限预算方案树” | researcher + critic + judge，quick-test/beam 淘汰 | G2 选方法 |
| 数学推导 | “把已选路线推导成方程、目标、约束和初边值，并检查量纲与极限情形” | deriver 生成可审计 MODEL_DERIVATION，critic/reviewer 复核 | — |
| 冻结模型 | “根据已审查推导起草 MODEL_SPEC 供审核” | 固化方程、约束、目标、评价和变更边界 | G3 冻结规格 |
| 建基线 | “按冻结规格制定求解器策略并实现可验证基线” | exact-first 策略、代码、测试、run record | G4 批准基线 |
| 优化 | “在固定模型下做参数调优、求解器比较和消融” | optimizer 写实验与结果，reviewer 复核 | — |
| 独立验证 | “做敏感性分析、鲁棒性验证和不确定性量化” | 独立验证集、seed/CI/UQ，validator/replicator 复核 | G5 冻结结果 |
| 制图 | “根据冻结结果制作论文图并做图级追溯” | 唯一视觉规范、Figure Contract、矢量图与 manifest | G6 批准图 |
| 写论文 | “从已验证 run 和 claim registry 撰写论文” | 数字→run→代码/输入/输出哈希追溯，写后独立审查 | — |
| 提交 | “检查匿名、格式、AI 声明和提交包” | compliance、package guard、最终校验 | G7 允许提交 |

“参数调优”和“模型验证”必须分开说、分开跑：前者用于改进，后者用于证明可靠，
不能用参与调参的数据再次冒充独立验证。

明确涉及 CUMCM/国赛论文时，系统会使用 `mm-cumcm-paper-writing-review` 的摘要、
格式工作清单和证据化评审流程；通用数模论文仍由 `mm-paper-writing` 负责。任何赛事的
格式、页数、文件大小和 AI 声明都必须以当届官方文件为准。

## 3. 七个人工 Gate

- G1：选哪道题；G2：采用哪套方法；G3：冻结 MODEL_SPEC；G4：接受 baseline；
  G5：冻结最终方法与结果；G6：批准最终图；G7：允许提交。
- 批准必须是队员的明确自然语言指令。Codex 会通过 `gate_control.py` 写入批准者、时间、
  说明和对应 artifact 哈希；方案树、代码、结果、图或论文发生实质变化时，相关 PASS
  及所有下游 PASS 自动失效。
- 若想退回，直接说：“撤销 G4，原因是……，把 G4 及后续 Gate 恢复 pending。”

## 4. 文件放在哪里

- `01_题目与要求/`：官方原始材料，只读。
- `02_参考文献/`：论文和外部资料，只读。
- `03_建模工作区/src/`：正式代码；`tests/`：测试；`runs/`：可复现运行；
  `results/`：正式结果；`figures/`：正式图。
- `04_论文与提交/paper/`：论文；`references/`：引用清单；
  `ai_provenance/`：AI 使用记录；`submission/`：最终提交物。
- `90_工具与配置/`：Skill、Agent、Hook、schema 和内部报告。比赛结果不要写到这里。

同一核心文件同时只能有一个 writer。researcher、critic、reviewer、validator、
replicator、judge、compliance 默认只读；implementer、optimizer、visualizer 只写各自职责区。

## 5. 结果为何可信

正式计算由 `tracked_run.py` 包装：运行前登记配置、seed、输入和代码哈希，运行后登记
stdout/stderr、输出哈希、指标、耗时和 Git 状态。论文中的计算结论必须在
`claim_registry.jsonl` 绑定有效 run；网页或 DOI 不能替代本项目的计算证据。

正式图只服从 `90_工具与配置/configs/VISUAL_STYLE_GUIDE.md`。打包程序会在创建 ZIP
之前拒绝 locked cache、内部状态、路径穿越、链接和未经 G7 批准的内容。

## 6. 队员自检与故障处理

在项目根目录可运行：

```powershell
./run.ps1 status
./run.ps1 validate
./run.ps1 test
./run.ps1 verify
./run.ps1 git-status
```

正常应看到结构、合同和测试 PASS。遇到失败时，把完整错误直接交给 Codex：

> 诊断并修复当前 validate 失败，只改确定错误，保留现有结果，修后重新验证。

不要手工改 `run-manifest.json` 的 Gate 状态，不要把“没找到解”写成“证明无解”，
不要编辑 `runs/*/manifest.json` 或已登记输出；参数、模型或结果改变时新建 run。

## 7. 最常用的自然语言模板

- “继续当前任务。先读状态和上次结果，只补尚未完成的步骤。”
- “并行做两条只读文献证据线，最后由 critic 检查矛盾，不开始写代码。”
- “按已冻结 MODEL_SPEC 实现 baseline，先小测试，记录 seed、参数、运行和失败原因。”
- “只做参数调优，不改数学定义；每个实验登记 run，保留负结果。”
- “对最终数字做独立复现和统计有效性检查，发现不一致先停止论文写作。”
- “从 verified claims 写论文，缺数字就列 blocker，不得补造。”

一句话原则：**人决定题目、模型和最终提交；Codex 自动组织执行、验证和留痕。**

## 8. 系统边界

- 自动路由在 Codex 会话中生效；直接在终端运行普通脚本不会自动分派 Skill 或 Agent。
- 比赛格式、匿名和 AI 声明必须先从官方规则填写到 `contest.yaml`，系统不会猜测。
- 自动测试只能证明工作流与合同按设计运行，不能代替队员对建模假设、方法选择和最终结论的判断。
- 新题开始时保留 `90_工具与配置/`，清空或归档上一题业务材料；不要把旧题的 Gate、run、结果或 claim 当作新题证据。
